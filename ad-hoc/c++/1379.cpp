#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    int a, b, c;
    while(cin >> a >> b && a != 0){
        c = (a-b)+a;
        cout<< c<< endl;
    }
    return 0;
}